<nav class="navbar navbar-expand-lg navbar-light bg-light shadow">

	<div class="container">
		<a class="navbar-brand" href="{{ path('index') }}">
			<img src="{{ asset('images/favicon.ico') }}" alt="Logo" style="width:40px;" class="rounded-pill">

		</a>
		<a class="navbar-brand" href="{{ path('index') }}">PHP Blog</a>

		<ul class="navbar-nav ms-auto">
			<li class="nav-item">
				<a class="nav-link" href="{{ path('category_index') }}">Categorías</a>
			</li>
			<li
				class="nav-item">{# <a class="nav-link" href='{{ path('user_create') }}'>Crear Usuario</a> #}
			</li>
			<li
				class="nav-item">{# <a class="nav-link" href="{{ path('comment_create', {'postId': post.id}) }}">Create Comment</a> #}
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('post_create') }}">Create Post</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('contact-v3') }}">Contact</a>
			</li>
		</ul>
	</div>
</nav>
